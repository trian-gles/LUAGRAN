-- script.lua

local granmodule = {}

function granmodule.init()
    state = {}
    -- setup initial values for the state at the start
    return tate;
end

function granmodule.generate(state)
    
    -- create parameters for a grain and modify state
    

    return rate, dur, freq, pan, state
end
return granmodule